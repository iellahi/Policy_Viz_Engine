---
title: "`r params$report_title`"
author: "`r params$report_author`"
date: "`r Sys.Date()`"
output:
  html_document:
    theme: flatly
    toc: false
    code_folding: hide
params:
  # Engine flag: TRUE only when this template is stitched into the
  # combined report (set by the parent). Leave false for normal renders.
  combined_mode: false
  # Master Data Path
  data_path: !r here::here("1_data", "master_macro_panel.csv")

  # Variables
  entity_var: "district"
  time_var: "year"        # set "" to skip time filtering entirely
  filter_time: 2025       # a year (works on raw dates/timestamps too); "" = keep all rows
  value_var: "literacy_rate"

  # Baseline: a number (policy target), or "mean" to benchmark against the
  # average across entities — no manual target needed.
  baseline_target: 65

  # Policy-Facing Labels
  metric_name: "Standardized Literacy Rate"

  # --- Report Text (all editable; leave "" to use the smart default) ---
  report_title: "Performance Outliers: Deviation Bar Chart"
  report_author: "Ibraheem Saqib Ellahi"
  chart_title: ""
  chart_subtitle: ""
  x_label: ""
  y_label: ""
  source_note: "Generated via CERP Analytics Template"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
  echo = !isTRUE(params$combined_mode),
  warning = FALSE,
  message = FALSE,
  dev.args = list(bg = "#FAF9F6"),
  fig.path = paste0(here::here("4_output", "figures"), "/"),
  fig.width = 9,
  fig.height = 6,
  fig.retina = 3
)
```

```{r cerp-style, echo=FALSE, results='asis', eval=!isTRUE(params$combined_mode)}
cat("<style>", readr::read_file(here::here("2_R", "cerp_style.css")), "</style>")
```

```{r load-and-prep}
library(tidyverse)
library(lubridate)
source(here::here("2_R", "2.5_helpers.R"), local = TRUE)

# Load data (headers and values whitespace-scrubbed) and validate YAML params.
# All data scrubbing + plotting now lives in viz_deviation() (2_R/2.6_viz_functions.R).
raw_data <- cerp_load(params$data_path)
cerp_validate(raw_data, params[grepl("_var$", names(params))])

# Build the plot object now so the chunk below can size fig.height from the
# number of entities it actually contains (p$data is the deviation frame).
source(here::here("2_R", "2.0_setup_theme.R"), local = TRUE)
source(here::here("2_R", "2.6_viz_functions.R"), local = TRUE)

p <- viz_deviation(
  data            = raw_data,
  entity_var      = params$entity_var,
  time_var        = params$time_var,
  filter_time     = params$filter_time,
  value_var       = params$value_var,
  baseline_target = params$baseline_target,
  metric_name     = params$metric_name,
  chart_title     = params$chart_title,
  chart_subtitle  = params$chart_subtitle,
  x_label         = params$x_label,
  y_label         = params$y_label,
  source_note     = params$source_note
)
```


```{r plot-deviation, fig.height=max(4, 1 + 0.45 * nrow(p$data))}
p
```
